# C0025 READ FIRST

C0025 is a review candidate, not a deploy payload.

The user approved the C0024 premium-restraint direction. C0025 keeps that rendered appearance while replacing the final hard-coded color contract with a primitive → semantic role → component token architecture.

C0024 remains the direct parent and visual rollback reference. C0022 remains the functional rollback reference.
