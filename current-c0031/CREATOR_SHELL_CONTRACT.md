# C0031 Creator Shell Contract

Scope only:

- `creator/` → Creator Hub: `body.c8.c`
- `creator/youtube/` → Creator Detail: `body.c8.s.y`
- `creator/editing/` → Creator Detail: `body.c8.s.e`

## Hub contract

- editorial split hero;
- one restrained primary CTA accent;
- three service choices presented as one decision rail rather than unrelated colored cards;
- status/progress presented as a quiet evidence rail;
- technical-support redirect remains separate.

## Creator Detail contract

YouTube and Editing reuse the approved service information architecture:

1. editorial hero + evidence rail;
2. three-option decision rail;
3. restrained section/navigation rhythm;
4. reduced card nesting for symptoms/features;
5. shared pricing rail;
6. mobile stacking without horizontal overflow.

## Invariants

- no price changes;
- no service-ID changes;
- no Apply URL changes;
- no application JavaScript changes;
- no policy/game/cases/reviews changes;
- no invented testimonials, customer counts, logos or performance claims.

Exact Candidate bytes are identified by SHA-256 `45d1b8d7fd70fcfe16ce450c6fe5ad4957b26c40cf4bb3b3924260a2863e3ddd`. The connector archive may not contain every binary byte; do not claim exact reconstruction from this contract alone.
