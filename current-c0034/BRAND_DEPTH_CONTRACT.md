# C0034 PC + Apply Brand Depth Contract

## Direct parent

C0033 Safety Acknowledgement Validation Fix.

## Target pages

- `pc/`
- `apply/`

## Visual contract

- keep C0031 information hierarchy;
- keep C0032 selective navy/cyan depth;
- one primary/recommended action gets the strongest accent;
- use elevation for a small number of decision anchors;
- group comparison content into coherent surfaces;
- avoid glow-heavy buttons, bright multi-color gradients, billboard typography and independent-card repetition.

## Functional contract

The C0033 safety fix is mandatory and must not change:
- unchecked `safety_ack` + `scope_ack` block summary;
- safety-only leaves `scope_ack` error;
- one availability time remains invalid;
- blocked submit preserves input;
- valid acknowledgements + two times generate summary;
- error-summary focus works.

## Immutable business data

- application options: 23
- application links: 86
- prices: preserved
- service IDs: preserved
- Apply URLs: preserved

## Scope exclusions

Do not change Home, Creator Hub, Rescue, Stream Ready, YouTube, Editing, policies, cases, reviews or remaining utility surfaces in this Candidate.
